import * as React from 'react';
import Style from './NotFound.module.scss'

function NotFound() {
    return (
        <div className={Style.boxNotFound}>
            未找到
        </div>
    );
}

export default NotFound;